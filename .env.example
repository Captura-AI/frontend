# Captura AI — Frontend environment

# Base URL of the NestJS backend API (note the global prefix `/api`).
# Local dev default matches backend APP_PORT=1337 + setGlobalPrefix('api').
NEXT_PUBLIC_API_BASE_URL=http://localhost:1337/api
